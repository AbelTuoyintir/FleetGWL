# TODO

- [x] Fix SQLite dashboard error: replace YEAR()/MONTH() usage in FuelMileageController@dashboard maintenanceTrend with SQLite-compatible expressions (strftime).
- [ ] Re-run the dashboard page to confirm the query no longer throws `no such function: YEAR`.


